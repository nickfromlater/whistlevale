name: Safari native review
on:
  pull_request:
    types: [opened, synchronize, reopened]
permissions:
  contents: read
jobs:
  review:
    if: github.head_ref == 'codex/safari-observatory' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: Integrate the reviewed native-room patch on this draft branch only
        run: |
          if test -f .github/safari-core.patch; then
            git apply --check .github/safari-core.patch
            git apply .github/safari-core.patch
            git rm .github/safari-core.patch
            git add index.html package.json src/hobby.js src/trains.js src/train-cabinet.js
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git commit -m 'Integrate safari room, dedicated stock and regression checks'
            git push origin HEAD:refs/heads/codex/safari-observatory
          fi
          mkdir -p evidence/safari
          git rev-parse HEAD > evidence/safari/reviewed-commit.txt
          git ls-tree -r HEAD > evidence/safari/reviewed-tree.txt
      - name: Install isolated reviewer browser, without changing project dependencies
        run: |
          npm install --prefix /tmp/whistlevale-browser playwright@1.55.1
          /tmp/whistlevale-browser/node_modules/.bin/playwright install --with-deps chromium
      - name: Review the actual native application
        env:
          PLAYWRIGHT_MODULE_PATH: /tmp/whistlevale-browser/node_modules/playwright/index.mjs
        run: |
          python3 scripts/serve.py --port 4175 > evidence/safari/server.log 2>&1 &
          node scripts/safari-visual-review.mjs 2>&1 | tee evidence/safari/browser-run.txt
      - name: Full project and geometry checks
        if: always()
        run: |
          npm test 2>&1 | tee evidence/safari/npm-test.txt
          npm run test:geometry:full 2>&1 | tee evidence/safari/geometry-full.txt
          npm run --silent check:contributions -- --json > evidence/safari/contributions.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: safari-native-visual-review
          path: |
            evidence/safari/*.png
            evidence/safari/*.json
            evidence/safari/*.txt
            evidence/safari/*.log
          retention-days: 7
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: safari-playable-house
          path: evidence/safari/whistlevale-safari-review.html
          if-no-files-found: ignore
          retention-days: 7
