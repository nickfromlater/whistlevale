'use strict';

// One brief for the visible Copy prompt action and `npm run bay -- <id>`.
// Resolve the catalogue when called so revised gallery layouts stay in sync.
function grandHallAgentPrompt(bayId){
 const id=String(bayId??'').trim().toUpperCase(),bay=GRAND_HALL_BAYS.find(item=>item.id===id);
 if(!bay)throw new RangeError('Unknown Grand Hall bay: '+id);
 const gallery=GRAND_HALL_GALLERIES[bay.room],width=bay.usableWidth??bay.width??bay.w,depth=bay.usableDepth??bay.depth??bay.d;
 const measure=n=>String(Number(n.toFixed(2)));
 const display={'open-table':'an open display table','low-vitrine':'a low glass case','tall-vitrine':'a tall glass case','wall-case':'a cabinet facing the aisle','round-vitrine':'a round glass case'}[bay.displayFormat]||'a display table';
 const envelope=bay.usableRadius?'a circular area '+measure(bay.usableRadius*2)+' metres across':bay.furniture==='round'?'a circular area '+measure(Math.min(width,depth))+' metres across':'an area '+measure(width)+' metres wide by '+measure(depth)+' metres deep';
 const maxHeight=bay.maxHeight??gallery.height-(bay.surfaceY??bay.y);
 return `Help me contribute an original miniature to Whistlevale, exhibition bay ${bay.id} in ${gallery.name}.

First, ask what I want to build and which public name or pseudonym to credit, unless I have already supplied them. A profile link is optional. Wait for any missing answer before authoring; do not choose a subject or invent my identity.

1. Work in https://github.com/nickfromlater/whistlevale. Inspect git status and preserve existing work. If needed, clone https://github.com/nickfromlater/whistlevale.git, then create a contribution branch from current main without discarding local changes. Read AGENTS.md, CONTRIBUTING.md, docs/contributing/grandhall.md, docs/contributing/geometry.md and docs/contributing/attribution.md. Node 24+ and Python 3.10+ are enough; no install, API key or recordings are required.

2. Run npm run bay -- ${bay.id} to confirm the current brief. Start an isolated local server with python3 scripts/serve.py --port 4175 (use another free port if occupied) and inspect /grandhall.html?bay=${bay.id}. This is ${gallery.category.toLowerCase()}: ${gallery.invitation} The bay's Hall position is x ${measure(bay.x)}, z ${measure(bay.z)}; its display surface is y ${measure(bay.surfaceY??bay.y)}. It has ${display}, rotated ${measure((bay.yaw??0)*180/Math.PI)}°. Its usable space is ${envelope}, with a maximum model height of ${measure(maxHeight)} metres above the display surface. Dimensions are shown to two decimal places; use the exact catalogue values when measuring geometry. These clear dimensions exclude the case frame: leave a margin and inspect any glazing, shaped corners, gallery architecture and adjacent sightlines before designing. Hall coordinates are not Commons coordinates: never put this placement in contributions/world.json. Selecting a bay neither reserves it nor publishes a contribution; verify it is still empty on current main.

3. Build the requested miniature as native procedural geometry in a new src/scenery/<slug>.js. Inspect src/scenery/willowbank.js and src/grandhall-exhibits.js. Both renderers support Builder primitives, ringX/ringZ, hash/shade and windowPane; do not assume other railway helpers or atlas labels exist in the Hall. Use supported material IDs (0,4,5,6,8,22,23,41) and test the actual model in both renderers. Author the requested shape and details, with intentional materials and a grounded, compact silhouette. Keep geometry deterministic, finite and static, with no new dependencies, third-party downloads or per-frame mesh generation. Model around a local origin; grandHallPlaceExhibit measures its lowest vertex and automatically grounds it at bay.surfaceY before applying the bay position, yaw and uniform scale. Do not add a second placement transform or vertical lift.

4. Declare the new source in grandhall.html before startup using a nonexecuting script with type="application/x-whistlevale-exhibit", data-source="src/scenery/<slug>.js" and data-src="src/scenery/<slug>.js". The Hall loads it only when needed and embeds it in portable exports. A Hall-only work does not need an index.html script; add an eager index.html script before hobby.js only for a railway placement or an explicitly reviewed mapPreview:true model. Add its callable mapping as a wrapper that resolves the builder only when invoked (so unloaded models are not referenced eagerly) to grandHallBuildExhibit in src/grandhall-exhibits.js, and add one exhibit record there with bay, unique id, title, credits, story, builder, scale and source path. Use the normal credits array: each credit has name and optional platform/handle and note, as described in the attribution guide. Include all original authors when adapting work; legacy maker/link fields are not the contract for new exhibits. grandHallExhibitCredits validates the array for the Hall, the main Builders list and export. Use my chosen credit and preserve safe profile links opening in a new tab. Do not overwrite an occupied bay, renumber bays or add a Commons placement unless I asked for one. Hall builders have their own registry; a Hall-only exhibit does not need COMMUNITY_BUILDERS or communityMiniatures changes. The record joins the public exhibition only after maintainer review and merge.

5. Fit the complete model, including foundations, roof overhangs and props, within this bay's usable area and maximum height. Check the emitted geometry after placement and scaling, including its height above the surface, not just its origin or the case's outer footprint. Keep the foundation in contact and leave clear space to the glass, frame and ceiling. Preserve walking clearance, neighboring exhibits, camera views, map scale, mesh disposal and the existing geometry budgets. Confirm the same native exhibit and its attribution appear in the entered Hall and the main house map.

6. Run npm run check:contributions -- --json, npm test and npm run test:geometry:full. Do not raise budgets to make checks pass. Inspect desktop, 390px and 320px layouts; direct bay links; bay selection by pointer and keyboard; close views of the miniature; the creator link; map entry/return; and runtime performance against the same camera before the change. Follow docs/contributing/review.md for export and credit checks affected by the change. Save screenshots and measurements under ignored evidence/<work-id>/ and mark unavailable physical-device or browser checks as unverified.

7. Prepare a focused commit and pull-request description using .github/PULL_REQUEST_TEMPLATE.md: requested subject, chosen credit and sources, bay ID, screenshots, measured geometry/performance impact, checks actually run and remaining limitations. Push or open the PR only when I authorize it. Do not merge, deploy or change repository visibility; a maintainer reviews the contribution.`;
}

// Future rooms have their own house-map plots, outside the Hall's exhibit bays.
function grandHallRoomPrompt(plot){
 if(!plot||typeof plot.id!=='string'||!/^[a-z][a-z0-9-]*$/.test(plot.id)||typeof plot.name!=='string'||!plot.name.trim()||typeof plot.side!=='string'||!plot.side.trim())throw new TypeError('A future room needs a valid plot id, name and side.');
 const side=plot.side==='left'?'west':plot.side==='right'?'east':plot.side;
 return `Help me propose and build an original Whistlevale room for ${plot.name}, house-map plot ${plot.id} on the ${side} side of the Grand Hall.

First, ask what kind of room I imagine, how it should feel, and which public name or pseudonym to credit, unless I have already supplied those answers. A profile link is optional. Confirm an accepted proposal for this whole room before implementing the larger project. Selecting this empty space does not reserve it or approve publication.

1. Work in https://github.com/nickfromlater/whistlevale. Inspect git status and preserve existing work; if needed, clone https://github.com/nickfromlater/whistlevale.git and create a contribution branch from current main. Read AGENTS.md, README.md, CONTRIBUTING.md (especially Adding a room and House layout metadata), docs/contributing/geometry.md, docs/contributing/attribution.md and docs/contributing/review.md. Run npm run contribute -- room. No install, runtime API key or recordings are required; use Node 24+ and Python 3.10+.

2. Inspect the actual house map and plot ${plot.id}, the Grand Hall beside it and the closest working room. Agree on a distinct architectural silhouette, landscape, lighting and a few intentional focal points. Let the accepted theme determine the room; do not duplicate a neighboring room, fill the space with generic clutter or shrink the Grand Hall to make it fit. Confirm this plot is still available on current main.

3. Implement src/rooms/<key>.js and registerHouseRoom('<key>', definition) using a stable lowercase room key. Supply the real build(scene, builder), a deliberate shell(builder), scene.height, camera spots and chosen credits. Set map.plot to '${plot.id}' so the room replaces this open site. Keep floors at FLOOR and define accurate map.footprint, uniform map.scale and map.focus for the actual architecture; a standard shell uses footprint [156,128] and scale .4, but author the correct dimensions for this room. Plot IDs encode a west/east wing and one-based row. The house registry owns placement, corridors, picking and camera bounds; do not hardcode world transforms or a new fixed room list. Preserve the authored physical scale and usable paths between the room and the Hall.

4. If the accepted room has a railway, build continuous supported tracks and populate scene.routes and scene.trains with valid edges, formations and matching steam/electric/diesel stock. Check wheels, bogies, roofs, train clearances and bridges. For a landscape-only room, explicitly use railway: false and leave routes and trains empty; keep landscape cinema and camera controls working. Use deterministic static geometry, finished terrain samplers and existing materials. Preserve mesh disposal, registry revisions and scene cache invalidation; avoid per-frame geometry and extra dependencies.

5. Include the new room module in index.html after its shared helpers and before atlas creation and startup. Register any atlas art within the existing capacity, and preserve all UV rebuild rules. Ensure the public build and portable export include every needed script. Preserve room discovery, preload behavior, sound category controls, quiet mobile controls and the map's entry/return flow. Keep the chosen creator credits and source notices visible and present in export; never invent a maker or overwrite another person's work.

6. Run npm run check:contributions -- --json, npm test and npm run test:geometry:full without raising budgets. Use python3 scripts/serve.py --port 4175 (or another free port) for an isolated preview. Inspect the complete house map and the entered room on desktop and 390px/320px phone widths: plot replacement, floor/corridor contact, room selection, orbit, cinema, moving trains if present, return navigation and creator links. Check playable export and relevant persistence/credit behavior using the review checklist. Measure geometry and performance against the same device and camera before the change; keep screenshots and notes in ignored evidence/<work-id>/ and mark unavailable physical-device checks as unverified.

7. Prepare a focused commit and pull-request description using .github/PULL_REQUEST_TEMPLATE.md, linking the accepted proposal and naming plot ${plot.id}. Include the requested theme, resulting room, chosen credits and sources, screenshots, measured impact, actual checks and remaining limitations. Push or open the PR only when I authorize it. Do not merge, deploy or change repository visibility; a maintainer reviews the room.`;
}
